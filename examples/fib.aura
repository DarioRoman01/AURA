fib := |n| => {
    si(n <= 1) {
        regresa n;
    }

    regresa fib(n - 1) + fib(n - 2);
}

numero := recibir_entero("Ingrese un numero para calcular su fibonacci: ")
resultado := fib(numero);
escribir(resultado);
