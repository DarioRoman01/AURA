var fib = funcion(n) {
    si(n <= 1) {
        regresa n;
    }

    regresa fib(n - 1) + fib(n - 2);
}

var numero = recibir_entero("Ingrese un numero para calcular su fibonacci: ")
var resultado = fib(numero);
escribir(resultado);
