var hola = funcion() {
    var mi_lista = lista[2,4,5,6,7]
    por(n en rango(largo(mi_lista))) {
        escribir("n es: ", mi_lista[n]);
    }
}

hola();

var insertar = funcion(array) {
    por(n en rango(10)) {
        array:agregar(n)
    }
}

var mi_lista = lista[2,2,3];
escribir(mi_lista);
escribir(mi_lista);
