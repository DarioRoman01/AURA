var hola = funcion() {
    var mi_lista = lista[2,4,5,6,7]
    por(n en rango(longitud(mi_lista))) {
        escribir("n es: ", mi_lista[n]);
    }
}

hola();

var func = funcion() {
    por(n en rango(10)) {
        si(n == 5) {
            regresa 5;
        }
    }

    regresa -1;
}

var resulado = func();
escribir(resulado);